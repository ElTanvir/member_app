import 'package:flutter/material.dart';

class TodaysFood extends StatelessWidget {
  const TodaysFood({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
